server:
  port: ${PORT:8080}

spring:
  application:
    name: todo-backend
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://todouser:Todo12345@cluster0.ibkd9yn.mongodb.net/tododb?retryWrites=true&w=majority}
      database: tododb

# JWT Configuration
jwt:
  secret: YourSuperSecretKeyForJWTTokenGenerationMustBe256BitsOrMore2024
  expiration: 86400000  # 24 hours in milliseconds

# Logging
logging:
  level:
    root: INFO
    com.todo: DEBUG
